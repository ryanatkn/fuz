/**
 * A function with basic parameters.
 *
 * @param name - The user's name
 * @param age - The user's age
 * @returns A greeting string
 */
export function greet(name: string, age: number): string {
	return `Hello ${name}, you are ${age} years old`;
}
