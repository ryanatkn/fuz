/**
 * Function demonstrating example usage.
 *
 * @example
 * const result = format_name('John', 'Doe');
 * console.log(result); // "Doe, John"
 */
export function format_name(first: string, last: string) {
	return `${last}, ${first}`;
}
