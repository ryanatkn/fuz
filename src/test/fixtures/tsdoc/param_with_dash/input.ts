/**
 * Function demonstrating dash separator in param descriptions.
 *
 * @param name the user's name
 * @param age the user's age
 */
export function format_user(name: string, age: number) {
	return `${name} (${age})`;
}
